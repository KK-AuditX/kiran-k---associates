# Environment Variables for Kiran K & Associates

# Email Configuration (Zoho Mail)
# Replace these with your actual Zoho Mail credentials

# Your Zoho Mail email address
EMAIL_USER=your-email@yourcompany.com

# Your Zoho Mail password or app password
EMAIL_PASSWORD=your-zoho-mail-password

# SMTP Server Configuration (defaults to Zoho)
SMTP_HOST=smtp.zoho.in
SMTP_PORT=587
SMTP_SECURE=false

# For Gmail, use these settings instead:
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# EMAIL_USER=your-gmail@gmail.com
# EMAIL_PASSWORD=your-16-char-app-password

# Note: Keep this file secure and never commit it to version control
# The .gitignore file already includes this file (.env)
